# Output field

Either "Density", "Overlap" or "Number of periods". If density then the maps show the density of records per period and level of identifier. If overlap then one map is returned per level of identifier showing which cells have been sampled in >= minPeriods periods. If "Number of periods" then one map is returned per level of identifier showing the number of periods in which each grid cell has been sampled.
