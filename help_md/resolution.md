# Resolution field

This metric requires calculation of each species commonness, e.g. the number of grid cells on which it has been recorded. Here the user can specify the spatial resolution of the grid cells used in this calculation. Units must match the units in your data, so if x and y coordinate are easting and northing, Resolution needs to be specified in meters.
